

num_inference_steps = 100

num_ax = 50

# for ai,i in enumerate(range(0,num_inference_steps,num_inference_steps // num_ax)):

#     print(f"ai={ai}, i={i}")

a = int(0.4*num_inference_steps)

print(f"a={a}")